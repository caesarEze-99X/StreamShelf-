import { type Config } from "prettier";

const configuration: Config = {
  semi: true,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: "all",
  printWidth: 100,
  bracketSpacing: true,
  arrowParens: "avoid",
};

export default configuration;
